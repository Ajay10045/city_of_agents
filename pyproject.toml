[project]
name = "city-of-agents"
version = "0.1.0"
description = "LLM-driven city simulation game server"
requires-python = ">=3.10"
dependencies = [
  "openai>=1.0.0",
  "anthropic>=0.34.0",
]

[dependency-groups]
dev = [
  "pytest>=8.0.0",
]

[tool.uv]
package = false
